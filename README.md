# Datastar Live Box Demo

A small Rust/Axum demo that uses Datastar over SSE to append text boxes, mark old boxes as exiting, and remove them after CSS has time to animate.

Run it with:

```sh
cargo run
```

Then open <http://127.0.0.1:3000>.
